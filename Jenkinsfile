pipeline {
    agent any

    stages {

      stage("build") {

        steps {
             echo "building code complete"
        }

      stage("test") {

        steps {
            echo "testing code complete"
        }

       stage("deploy") {

        steps {
          echo "deploying code complete"
        }
      }
    }
}
